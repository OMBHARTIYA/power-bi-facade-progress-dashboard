# Publish Checklist

- Review `README.md` and `docs/kpis.md` for final wording.
- Open both `.pbix` files once and confirm they do not contain private credentials or internal-only links.
- Confirm the screenshots in `screenshots/` are the ones you want to present publicly.
- Confirm `docs/data-model.png` is safe to share.
- Double-check that no raw source folders, passkeys, or project ID text files were copied into this clean repo.
- Initialize a new Git repo in this folder.
- Create a new GitHub repository with the same clean project name.
- Commit the files and push.

## Suggested Repo Name

`power-bi-facade-progress-dashboard`
